node_modules/
.claude/

# Local env & secrets
.env
.env.*
!.env.example

# Vercel
.vercel

# Legacy SQLite scratch DBs
data/*.db
data/*.db-shm
data/*.db-wal

# Seed/sample JSON (moved to DB; kept out of VCS to prevent PII leaks)
data/*.json

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo

# Misc artefacts that should never ship
*.zip
